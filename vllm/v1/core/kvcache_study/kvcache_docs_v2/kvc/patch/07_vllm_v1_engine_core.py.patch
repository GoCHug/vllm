diff --git a/vllm/v1/engine/core.py b/vllm/v1/engine/core.py
index 08c814ab34..6141038829 100644
--- a/vllm/v1/engine/core.py
+++ b/vllm/v1/engine/core.py
@@ -24,7 +24,7 @@ import vllm.envs as envs
 from vllm.config import ParallelConfig, VllmConfig
 from vllm.distributed import (
     cleanup_dist_env_and_memory,
-    stateless_destroy_torch_distributed_process_group,
+    stateless_destroy_torch_distributed_process_group
 )
 from vllm.envs import enable_envs_cache
 from vllm.logger import init_logger
@@ -37,7 +37,7 @@ from vllm.transformers_utils.config import maybe_register_config_serialize_by_va
 from vllm.utils import numa_utils
 from vllm.utils.gc_utils import (
     freeze_gc_heap,
-    maybe_attach_gc_debug_callback,
+    maybe_attach_gc_debug_callback
 )
 from vllm.utils.hashing import get_hash_fn_by_name
 from vllm.utils.network_utils import make_zmq_socket
@@ -48,7 +48,7 @@ from vllm.v1.core.kv_cache_utils import (
     get_kv_cache_configs,
     get_request_block_hasher,
     init_none_hash,
-    resolve_kv_cache_block_sizes,
+    resolve_kv_cache_block_sizes
 )
 from vllm.v1.core.sched.interface import PauseState, SchedulerInterface
 from vllm.v1.core.sched.output import SchedulerOutput
@@ -66,14 +66,14 @@ from vllm.v1.engine import (
     ReconfigureDistributedRequest,
     ReconfigureRankType,
     UtilityOutput,
-    UtilityResult,
+    UtilityResult
 )
 from vllm.v1.engine.tensor_ipc import TensorIpcReceiver
 from vllm.v1.engine.utils import (
     EngineHandshakeMetadata,
     EngineZmqAddresses,
     SignalCallback,
-    get_device_indices,
+    get_device_indices
 )
 from vllm.v1.executor import Executor
 from vllm.v1.kv_cache_interface import KVCacheConfig, get_kv_cache_spec_kind
@@ -101,7 +101,7 @@ class EngineCore:
         executor_class: type[Executor],
         log_stats: bool,
         executor_fail_callback: Callable | None = None,
-        include_finished_set: bool = False,
+        include_finished_set: bool = False
     ):
         # plugins need to be loaded at the engine/scheduler level too
         from vllm.plugins import load_general_plugins
@@ -113,7 +113,7 @@ class EngineCore:
             logger.info(
                 "Initializing a V1 LLM engine (v%s) with config: %s",
                 VLLM_VERSION,
-                vllm_config,
+                vllm_config
             )
 
         self.log_stats = log_stats
@@ -153,7 +153,7 @@ class EngineCore:
             include_finished_set=include_finished_set,
             log_stats=self.log_stats,
             block_size=scheduler_block_size,
-            hash_block_size=hash_block_size,
+            hash_block_size=hash_block_size
         )
         self.use_spec_decode = vllm_config.speculative_config is not None
         if self.scheduler.connector is not None:  # type: ignore
@@ -261,6 +261,11 @@ class EngineCore:
             available_gpu_memory = [0] * len(kv_cache_specs)
 
         assert len(kv_cache_specs) == len(available_gpu_memory)
+        # [KVC][CFG] 配置侧·测预算: 各 worker profile_run 实测可用 KV 显存 (以实际跑为准)
+        logger.info(
+            f"[KVC][CFG] determine_available_memory: 各 worker 可用 KV 显存 = "
+            f"{[f'{m / 1024**3:.2f}GiB' for m in available_gpu_memory]}"
+        )
 
         # Track max_model_len before KV cache config to detect auto-fit changes
         max_model_len_before = vllm_config.model_config.max_model_len
@@ -268,6 +273,33 @@ class EngineCore:
         kv_cache_configs = get_kv_cache_configs(
             vllm_config, kv_cache_specs, available_gpu_memory
         )
+        # [KVC][CFG] 配置侧·编排产物: KVCacheSpec -> KVCacheGroupSpec -> KVCacheTensor -> KVCacheConfig
+        for _w_idx, _cfg in enumerate(kv_cache_configs):
+            logger.info(
+                f"[KVC][CFG] worker{_w_idx} KVCacheConfig: num_blocks={_cfg.num_blocks}, "
+                f"groups数={len(_cfg.kv_cache_groups)}, tensors数={len(_cfg.kv_cache_tensors)}"
+            )
+            for _g_idx, _g in enumerate(_cfg.kv_cache_groups):
+                logger.info(
+                    f"[KVC][CFG]   [{_w_idx}] KVCacheGroupSpec(group_id={_g_idx}): "
+                    f"layers={len(_g.layer_names)} (首层 {_g.layer_names[0]}, 末层 {_g.layer_names[-1]}), "
+                    f"is_eagle_group={_g.is_eagle_group}"
+                )
+                logger.info(
+                    f"[KVC][CFG]   [{_w_idx}]   kv_cache_spec={repr(_g.kv_cache_spec)}"
+                )
+                _spec = _g.kv_cache_spec
+                logger.info(
+                    f"[KVC][CFG]   [{_w_idx}]   page_size_bytes={_spec.page_size_bytes} "
+                    f"({_spec.page_size_bytes / 1024:.1f}KB/层/块), "
+                    f"storage_block_size={_spec.storage_block_size}"
+                )
+            for _t in _cfg.kv_cache_tensors:
+                logger.info(
+                    f"[KVC][CFG]   [{_w_idx}] KVCacheTensor: size={_t.size} bytes "
+                    f"({_t.size / 1024**2:.2f}MiB), shared_by={len(_t.shared_by)} 层 "
+                    f"({_t.shared_by[0]} .. {_t.shared_by[-1]})"
+                )
 
         # If auto-fit reduced max_model_len, sync the new value to workers.
         # This is needed because workers were spawned before memory profiling
@@ -279,6 +311,12 @@ class EngineCore:
         scheduler_kv_cache_config = generate_scheduler_kv_cache_config(kv_cache_configs)
         vllm_config.cache_config.num_gpu_blocks = scheduler_kv_cache_config.num_blocks
         kv_cache_groups = scheduler_kv_cache_config.kv_cache_groups
+        # [KVC][CFG] 多 worker min(num_blocks) 对齐后的最终逻辑块数 (下发逻辑侧 BlockPool)
+        logger.info(
+            f"[KVC][CFG] 最终 scheduler KVCacheConfig: num_blocks={scheduler_kv_cache_config.num_blocks} "
+            f"(跨 worker min 对齐), cache_config.num_gpu_blocks={vllm_config.cache_config.num_gpu_blocks}, "
+            f"block_size={vllm_config.cache_config.block_size}"
+        )
         if kv_cache_groups:
             vllm_config.cache_config.block_size = min(
                 g.kv_cache_spec.block_size for g in kv_cache_groups
@@ -300,19 +338,19 @@ class EngineCore:
                 elapsed,
                 compile_time + encoder_compile_time,
                 compile_time,
-                encoder_compile_time,
+                encoder_compile_time
             )
         elif compile_time > 0:
             logger.info_once(
                 "init engine (profile, create kv cache, warmup model) took "
                 "%.2f s (compilation: %.2f s)",
                 elapsed,
-                compile_time,
+                compile_time
             )
         else:
             logger.info_once(
                 "init engine (profile, create kv cache, warmup model) took %.2f s",
-                elapsed,
+                elapsed
             )
         return scheduler_kv_cache_config
 
@@ -456,7 +494,7 @@ class EngineCore:
         grammar_output = self.scheduler.get_grammar_bitmask(scheduler_output)
         with (
             self.log_error_detail(scheduler_output),
-            self.log_iteration_details(scheduler_output),
+            self.log_iteration_details(scheduler_output)
         ):
             model_output = future.result()
             if model_output is None:
@@ -482,7 +520,7 @@ class EngineCore:
                 self.scheduler.update_draft_token_ids(draft_token_ids)
 
     def step_with_batch_queue(
-        self,
+        self
     ) -> tuple[dict[int, EngineCoreOutputs] | None, bool]:
         """Schedule and execute batches with the batch queue.
         Note that if nothing to output in this step, None is returned.
@@ -555,7 +593,7 @@ class EngineCore:
         future, scheduler_output, exec_model_fut = batch_queue.pop()
         with (
             self.log_error_detail(scheduler_output),
-            self.log_iteration_details(scheduler_output),
+            self.log_iteration_details(scheduler_output)
         ):
             model_output = future.result()
             if model_output is None:
@@ -673,14 +711,14 @@ class EngineCore:
     def _reset_caches(
         self,
         reset_running_requests: bool = True,
-        reset_connector: bool = True,
+        reset_connector: bool = True
     ) -> None:
         # reset_connector=True so external connectors clear alongside
         # local caches, matching the pause_generation(clear_cache=True)
         # contract. No-op when no connector is configured.
         self.reset_prefix_cache(
             reset_running_requests=reset_running_requests,
-            reset_connector=reset_connector,
+            reset_connector=reset_connector
         )
         self.reset_mm_cache()
         self.reset_encoder_cache()
@@ -801,7 +839,7 @@ class EngineCore:
         self,
         path: str,
         pattern: str | None = None,
-        max_size: int | None = None,
+        max_size: int | None = None
     ) -> None:
         self.model_executor.save_sharded_state(
             path=path, pattern=pattern, max_size=max_size
@@ -812,7 +850,7 @@ class EngineCore:
         method: str | Callable[..., _R],
         timeout: float | None = None,
         args: tuple = (),
-        kwargs: dict[str, Any] | None = None,
+        kwargs: dict[str, Any] | None = None
     ) -> list[_R]:
         return self.model_executor.collective_rpc(method, timeout, args, kwargs)
 
@@ -846,7 +884,7 @@ class EngineCore:
     def _eep_send_engine_core_notification(
         self,
         notification_type: EEPNotificationType,
-        vllm_config: VllmConfig | None = None,
+        vllm_config: VllmConfig | None = None
     ):
         raise NotImplementedError
 
@@ -874,7 +912,7 @@ class EngineCoreProc(EngineCore):
         client_handshake_address: str | None = None,
         tensor_queue: Queue | None = None,
         *,
-        engine_index: int = 0,
+        engine_index: int = 0
     ):
         self.input_queue = queue.Queue[tuple[EngineCoreRequestType, Any]]()
         self.output_queue = queue.Queue[tuple[int, EngineCoreOutputs] | bytes]()
@@ -898,7 +936,7 @@ class EngineCoreProc(EngineCore):
             identity,
             local_client,
             vllm_config,
-            client_handshake_address,
+            client_handshake_address
         ) as addresses:
             # Set up data parallel environment.
             self.has_coordinator = addresses.coordinator_output is not None
@@ -908,7 +946,7 @@ class EngineCoreProc(EngineCore):
             logger.debug(
                 "Has DP Coordinator: %s, stats publish address: %s",
                 self.has_coordinator,
-                self.frontend_stats_publish_address,
+                self.frontend_stats_publish_address
             )
             internal_dp_balancing = (
                 self.has_coordinator
@@ -923,7 +961,7 @@ class EngineCoreProc(EngineCore):
             if envs.VLLM_ELASTIC_EP_SCALE_UP_LAUNCH:
                 self._eep_send_engine_core_notification(
                     EEPNotificationType.NEW_CORE_ENGINES_INIT_READY,
-                    vllm_config=vllm_config,
+                    vllm_config=vllm_config
                 )
             self._init_data_parallel(vllm_config)
 
@@ -932,7 +970,7 @@ class EngineCoreProc(EngineCore):
                 executor_class,
                 log_stats,
                 executor_fail_callback,
-                internal_dp_balancing,
+                internal_dp_balancing
             )
 
             # Background Threads and Queues for IO. These enable us to
@@ -947,9 +985,9 @@ class EngineCoreProc(EngineCore):
                     addresses.inputs,
                     addresses.coordinator_input,
                     identity,
-                    ready_event,
+                    ready_event
                 ),
-                daemon=True,
+                daemon=True
             )
             input_thread.start()
 
@@ -958,9 +996,9 @@ class EngineCoreProc(EngineCore):
                 args=(
                     addresses.outputs,
                     addresses.coordinator_output,
-                    self.engine_index,
+                    self.engine_index
                 ),
-                daemon=True,
+                daemon=True
             )
             self.output_thread.start()
 
@@ -979,7 +1017,7 @@ class EngineCoreProc(EngineCore):
         identity: bytes,
         local_client: bool,
         vllm_config: VllmConfig,
-        client_handshake_address: str | None,
+        client_handshake_address: str | None
     ) -> Generator[EngineZmqAddresses, None, None]:
         """
         Perform startup handshakes.
@@ -1013,7 +1051,7 @@ class EngineCoreProc(EngineCore):
             is_local,
             headless,
             vllm_config,
-            vllm_config.parallel_config,
+            vllm_config.parallel_config
         )
         if client_handshake_address is None:
             # We only need to handshake with one party.
@@ -1046,7 +1084,7 @@ class EngineCoreProc(EngineCore):
         local_client: bool,
         headless: bool,
         vllm_config: VllmConfig,
-        parallel_config_to_update: ParallelConfig | None = None,
+        parallel_config_to_update: ParallelConfig | None = None
     ) -> Generator[EngineZmqAddresses, None, None]:
         with make_zmq_socket(
             ctx,
@@ -1054,7 +1092,7 @@ class EngineCoreProc(EngineCore):
             zmq.DEALER,
             identity=identity,
             linger=5000,
-            bind=False,
+            bind=False
         ) as handshake_socket:
             # Register engine with front-end.
             addresses = self.startup_handshake(
@@ -1081,7 +1119,7 @@ class EngineCoreProc(EngineCore):
         handshake_socket: zmq.Socket,
         local_client: bool,
         headless: bool,
-        parallel_config: ParallelConfig | None = None,
+        parallel_config: ParallelConfig | None = None
     ) -> EngineZmqAddresses:
         # Send registration message.
         handshake_socket.send(
@@ -1146,7 +1184,7 @@ class EngineCoreProc(EngineCore):
                 )
                 logger.debug(
                     "Setting kv_transfer_config.engine_id to %s",
-                    vllm_config.kv_transfer_config.engine_id,
+                    vllm_config.kv_transfer_config.engine_id
                 )
 
             parallel_config.data_parallel_index = dp_rank
@@ -1177,7 +1215,7 @@ class EngineCoreProc(EngineCore):
                 signal_name = signal.Signals(signum).name
                 logger.info(
                     "[shutdown] EngineCore: trigger received signal=%s",
-                    signal_name,
+                    signal_name
                 )
                 engine_core.shutdown_state = EngineShutdownState.REQUESTED
                 signal_callback.trigger()
@@ -1297,7 +1335,7 @@ class EngineCoreProc(EngineCore):
             logger.info(
                 "[shutdown] EngineCore: start mode=%s timeout=%ds",
                 mode,
-                shutdown_timeout,
+                shutdown_timeout
             )
 
             if shutdown_timeout == 0:
@@ -1305,7 +1343,7 @@ class EngineCoreProc(EngineCore):
                 if num_requests > 0:
                     logger.info(
                         "[shutdown] EngineCore: aborting in-flight requests count=%d",
-                        num_requests,
+                        num_requests
                     )
                 aborted_reqs = self.scheduler.finish_requests(
                     None, RequestStatus.FINISHED_ABORTED
@@ -1318,7 +1356,7 @@ class EngineCoreProc(EngineCore):
                         "[shutdown] EngineCore: draining in-flight requests "
                         "count=%d timeout=%ds",
                         num_requests,
-                        shutdown_timeout,
+                        shutdown_timeout
                     )
 
             self.shutdown_state = EngineShutdownState.SHUTTING_DOWN
@@ -1374,7 +1412,7 @@ class EngineCoreProc(EngineCore):
 
         logger.debug(
             "[shutdown] EngineCore: rejecting new request request_id=%s",
-            request.request_id,
+            request.request_id
         )
         self._send_abort_outputs_to_client([request.request_id], request.client_index)
         return True
@@ -1387,7 +1425,7 @@ class EngineCoreProc(EngineCore):
 
         logger.warning(
             "[shutdown] EngineCore: rejecting utility call method=%s",
-            method_name,
+            method_name
         )
         output = UtilityOutput(call_id, failure_message="Server shutting down")
         self.output_queue.put_nowait(
@@ -1450,7 +1488,7 @@ class EngineCoreProc(EngineCore):
         input_addresses: list[str],
         coord_input_address: str | None,
         identity: bytes,
-        ready_event: threading.Event,
+        ready_event: threading.Event
     ):
         """Input socket IO thread."""
 
@@ -1478,7 +1516,7 @@ class EngineCoreProc(EngineCore):
                         coord_input_address,
                         zmq.XSUB,
                         identity=identity,
-                        bind=False,
+                        bind=False
                     )
                 )
                 # Send subscription message to coordinator.
@@ -1492,7 +1530,7 @@ class EngineCoreProc(EngineCore):
                 block_size=self.vllm_config.cache_config.block_size,
                 dp_stats_address=self.frontend_stats_publish_address,
                 dtype=str(self.vllm_config.model_config.dtype).removeprefix("torch."),
-                vllm_version=VLLM_VERSION,
+                vllm_version=VLLM_VERSION
             )
             ready_payload = msgspec.msgpack.encode(ready_response)
             for input_socket in input_sockets:
@@ -1710,7 +1748,7 @@ class DPEngineCoreProc(EngineCoreProc):
         executor_class: type[Executor],
         log_stats: bool,
         client_handshake_address: str | None = None,
-        tensor_queue: Queue | None = None,
+        tensor_queue: Queue | None = None
     ):
         assert vllm_config.model_config.is_moe, (
             "DPEngineCoreProc should only be used for MoE models"
@@ -1744,7 +1782,7 @@ class DPEngineCoreProc(EngineCoreProc):
             log_stats,
             client_handshake_address,
             engine_index=dp_rank,
-            tensor_queue=tensor_queue,
+            tensor_queue=tensor_queue
         )
 
     def _init_data_parallel(self, vllm_config: VllmConfig):
@@ -1847,7 +1885,7 @@ class DPEngineCoreProc(EngineCoreProc):
                 if not self.engines_running:
                     logger.debug(
                         "EngineCore starting idle loop for wave %d.",
-                        new_wave,
+                        new_wave
                     )
                     self.engines_running = True
         else:
@@ -1916,7 +1954,7 @@ class DPEngineCoreProc(EngineCoreProc):
                     self.output_queue.put_nowait(
                         (
                             client_index,
-                            EngineCoreOutputs(wave_complete=self.current_wave),
+                            EngineCoreOutputs(wave_complete=self.current_wave)
                         )
                     )
                 # Increment wave count and reset step counter.
@@ -1934,7 +1972,7 @@ class DPEngineCoreProc(EngineCoreProc):
         has_unfinished, pause_consensus = ParallelConfig.sync_dp_state(
             self.dp_group,
             has_unfinished=local_unfinished,
-            pending_pause=self.pending_pause,
+            pending_pause=self.pending_pause
         )
 
         if pause_consensus:
@@ -1985,7 +2023,7 @@ class DPEngineCoreProc(EngineCoreProc):
             new_parallel_config=new_parallel_config,
             worker_type="removing" if is_shutdown else "existing",
             scale_type="scale_down" if is_scale_down else "scale_up",
-            reconfig_request=reconfig_request,
+            reconfig_request=reconfig_request
         )
         self.process_input_queue_block = False
         logger.info(
@@ -1995,7 +2033,7 @@ class DPEngineCoreProc(EngineCoreProc):
     def _eep_send_engine_core_notification(
         self,
         notification_type: EEPNotificationType,
-        vllm_config: VllmConfig | None = None,
+        vllm_config: VllmConfig | None = None
     ):
         """
         Send notifications to EngineCoreClient, which can then forward
@@ -2015,7 +2053,7 @@ class DPEngineCoreProc(EngineCoreProc):
         outputs = EngineCoreOutputs(
             utility_output=UtilityOutput(
                 call_id=EEP_NOTIFICATION_CALL_ID,
-                result=UtilityResult(notification_data),
+                result=UtilityResult(notification_data)
             )
         )
         outputs.engine_index = self.engine_index
@@ -2028,7 +2066,7 @@ class DPEngineCoreProc(EngineCoreProc):
                 zmq.Context() as ctx,
                 make_zmq_socket(
                     ctx, self.addresses.outputs[0], zmq.PUSH, linger=4000
-                ) as socket,
+                ) as socket
             ):
                 socket.send_multipart(encoder.encode(outputs))
 
@@ -2054,7 +2092,7 @@ class DPEngineCoreProc(EngineCoreProc):
             new_parallel_config=self.vllm_config.parallel_config,
             worker_type="new",
             scale_type="scale_up",
-            reconfig_request=None,
+            reconfig_request=None
         )
         self.eep_scaling_state.run_pre_kv_init_states()
         self.process_input_queue_block = False
@@ -2070,13 +2108,13 @@ class EngineCoreActorMixin:
         vllm_config: VllmConfig,
         addresses: EngineZmqAddresses,
         dp_rank: int = 0,
-        local_dp_rank: int = 0,
+        local_dp_rank: int = 0
     ):
         # Initialize tracer for distributed tracing if configured.
         maybe_init_worker_tracer(
             instrumenting_module_name="vllm.engine_core",
             process_kind="engine_core",
-            process_name=f"DPEngineCoreActor_DP{dp_rank}",
+            process_name=f"DPEngineCoreActor_DP{dp_rank}"
         )
 
         self.addresses = addresses
@@ -2150,7 +2188,7 @@ class EngineCoreActorMixin:
         identity: bytes,
         local_client: bool,
         vllm_config: VllmConfig,
-        client_handshake_address: str | None,
+        client_handshake_address: str | None
     ):
         """
         For Ray, we don't need to actually perform handshake.
@@ -2196,7 +2234,7 @@ class DPMoEEngineCoreActor(EngineCoreActorMixin, DPEngineCoreProc):
         executor_class: type[Executor],
         log_stats: bool,
         dp_rank: int = 0,
-        local_dp_rank: int = 0,
+        local_dp_rank: int = 0
     ):
         vllm_config.parallel_config.data_parallel_rank = dp_rank
 
@@ -2219,7 +2257,7 @@ class EngineCoreActor(EngineCoreActorMixin, EngineCoreProc):
         executor_class: type[Executor],
         log_stats: bool,
         dp_rank: int = 0,
-        local_dp_rank: int = 0,
+        local_dp_rank: int = 0
     ):
         vllm_config.parallel_config.data_parallel_size = 1
         vllm_config.parallel_config.data_parallel_size_local = 1
@@ -2235,5 +2273,5 @@ class EngineCoreActor(EngineCoreActorMixin, EngineCoreProc):
             "",
             executor_class,
             log_stats,
-            engine_index=dp_rank,
+            engine_index=dp_rank
         )
